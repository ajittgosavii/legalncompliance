# PWE Compliance AI - Dependencies
# Core
streamlit>=1.40.0
python-dotenv>=1.0.0

# LLM & AI
anthropic>=0.40.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# Vector Store — using built-in in-memory TF-IDF for Streamlit Cloud compatibility
# chromadb>=0.5.0              # Uncomment for local/production (requires compatible protobuf)
# langchain-chroma>=0.2.0     # Uncomment for local/production

# Embeddings — using built-in tokenizer for Streamlit Cloud compatibility
# sentence-transformers>=3.0.0  # Uncomment for local (heavy dependency)
# langchain-voyageai>=0.3.0    # Uncomment if using Voyage Law-2
# langchain-openai>=0.2.0      # Uncomment if using OpenAI embeddings

# Data & Visualization
plotly>=5.24.0
pandas>=2.2.0

# Web Scraping (for production scrapers)
# beautifulsoup4>=4.12.0
# playwright>=1.48.0
# requests>=2.32.0

# Document Parsing (for production)
# unstructured>=0.16.0
# pypdf>=5.0.0
